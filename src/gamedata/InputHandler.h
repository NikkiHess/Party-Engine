#pragma once

// std library
#include <unordered_map>
#include <vector>

// dependencies
#include "SDL2/SDL.h"

enum InputState {
	UP, NEWLY_UP, DOWN, NEWLY_DOWN 
};

class InputHandler {
public:
	
};